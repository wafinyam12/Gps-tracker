{
  "name": "sales-daily-workspace",
  "private": true,
  "scripts": {
    "backend:build": "npm --prefix gps-tracker run build",
    "mobile:check": "npm --prefix gps-tracker-mobile run check"
  }
}
